#include<iostream>
#include<algorithm>
using namespace std;

int main(){
  string S;
  cin >> S;

  int count=0; string num;
  for(int i=0;i<S.size();i++){
    for(int j=0;j<S.size();j++){
      vector<string>mid(S.size());
      for(int k=0;k<S.size();k++){
        mid.at(k)=S.at()
      }
      num += num + S.at();
      count
    }
  }
}
